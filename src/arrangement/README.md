# Arrangement Area

**Owner:** Arrangement Agent

## 职责

- 音乐生成规则
- Agent prompt 设计
- 工具/动作定义
- 确定性 fallback 生成器
- 输出解释生成

## 不负责

- 共享类型定义 (在 `src/contracts/`)
- 前端 UI 实现 (在 `src/frontend/`)
- 后端 API 实现 (在 `src/backend/`)
