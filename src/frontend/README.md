# Frontend Area

**Owner:** Frontend Agent

## 职责

- React 组件和 UI 实现
- 用户输入捕获
- Tone.js 播放调度
- 客户端状态管理
- 调用后端 API

## 不负责

- 共享类型定义 (在 `src/contracts/`)
- 后端 API 实现 (在 `src/backend/`)
- 编曲生成逻辑 (在 `src/arrangement/`)
