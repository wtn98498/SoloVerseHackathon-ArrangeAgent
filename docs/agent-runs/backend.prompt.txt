Role: App Backend Agent

Start by declaring:
Role, Read files, Owns, Does not own, First milestone, Validation, Commit plan.

Read first:
1. CLAUDE.md
2. PLAN.md
3. docs/contracts.md
4. docs/team-split.md
5. docs/agent-backend.md

Use the installed Matt Pocock engineering skills from the shared skills
directory if available, especially for TypeScript structure, validation, and
testable modules.

Mission:
Build a stable local service layer that works without a model key and can be
called by the frontend.

Owns:
- src/backend/
- backend-facing API adapter files
- validation utilities if placed under src/backend/
- may add shared exports only if needed without changing contracts

Does not own:
- src/frontend/
- src/arrangement/ generation internals
- UI styling

Implementation requirements:
- Keep the browser demo safe: no API key must still return fallback results.
- Implement functions equivalent to:
  - completeArrangementEndpoint({ seed, currentProject? })
  - energyEndpoint({ project, direction })
- Use the API shapes from src/contracts/api.ts and docs/contracts.md.
- Add validation for ArrangementProject and SeedPattern.
- Ensure fallback results always include four tracks and pass 8-bar/128-step
  rules.
- Add a DeepSeek wrapper boundary, but do not let missing DEEPSEEK_API_KEY
  break the flow.
- Timeouts and malformed model output should route to fallback.
- If the app is client-only for now, expose backend logic as TypeScript service
  functions that frontend can import during the hackathon.

Validation:
- npm run typecheck
- npm run build
- Add focused tests if the existing setup supports it without large tool churn;
  otherwise add small self-check helpers and document how they were verified.

Commit:
Commit only your scoped work with a Chinese commit message.

Final report:
Include status, changed files, verification, commit hash, blockers, and next
recommendation.

