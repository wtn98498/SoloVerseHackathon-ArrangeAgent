Role: Frontend Agent

Start by declaring:
Role, Read files, Owns, Does not own, First milestone, Validation, Commit plan.

Read first:
1. CLAUDE.md
2. PLAN.md
3. docs/contracts.md
4. docs/team-split.md
5. docs/agent-frontend.md

Use the installed frontend design skill(s) from the shared skills directory if
available. The intended design language is Nintendo-inspired music toy:
playful, tactile, colorful, big controls, clear feedback, not a generic AI
dashboard and not a dense DAW.

Mission:
Build the first browser-runnable playable editor slice.

Owns:
- src/frontend/
- src/App.tsx
- src/App.css
- src/index.css only if needed for layout reset

Does not own:
- src/backend/
- src/arrangement/
- src/contracts/ unless a compile fix is unavoidable

Implementation requirements:
- Keep using the existing Vite + React + TypeScript baseline.
- Render the four tracks from the fixture or app state.
- Implement play/stop UI and an 8-bar visual playback cursor.
- Use Tone.js for basic audible playback if feasible within the first attempt;
  if audio blocks progress, keep UI playback state working and report the
  blocker.
- Add toy-like drum and keys controls first. Bass/guitar can be simpler.
- Capture user input into a SeedPattern-shaped local state.
- Add right-side Agent panel with Complete, More Energy, Softer buttons.
- If backend endpoints are not ready, use a local mock adapter with the exact
  API response shape from docs/contracts.md.
- Treat source: "fallback" as a normal successful response.
- Keep visible text minimal and product-like.

Validation:
- npm run typecheck
- npm run build
- If possible, run the app and confirm the UI renders.

Commit:
Commit only your scoped work with a Chinese commit message.

Final report:
Include status, changed files, verification, commit hash, blockers, and next
recommendation.

